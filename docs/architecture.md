# Architecture

Placeholder for Step 0.

This document will describe:

- Feature and layer boundaries (`app`, `domain`, `features`, `services`, `shared`)
- Clinical note state machine responsibilities
- Offline / persistence strategy (Dexie)
- Sync and conflict considerations
- Testing strategy across unit, integration, and e2e layers

No domain modelling is implemented yet. The current codebase is an application shell only.
